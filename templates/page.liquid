 → {% comment %}
  Page Template
{% endcomment %}

<div class="page-width">
  <div class="page">
    <h1 class="h1">{{ page.title }}</h1>
    <div class="page-content">
      {{ page.content }}
    </div>
  </div>
</div>